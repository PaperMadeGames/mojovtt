<form autocomplete="off" onsubmit="event.preventDefault();">
    {{> "mojo.trait-list" prefix="choices"}}

    {{#if customPath}}
        <div class="form-group stacked">
            <label>{{localize "MOJO.TraitSelectorSpecial"}}</label>
            <input type="text" name="{{customPath}}" value="{{custom}}">
        </div>
    {{/if}}

    {{#if bypassesPath}}
        <h3>{{localize "MOJO.DamagePhysicalBypass"}}</h3>
        <p class="note">{{localize "MOJO.DamagePhysicalBypassHint"}}</p>
        {{> "mojo.trait-list" choices=bypasses prefix="bypasses"}}
    {{/if}}

    <button type="submit"><i class="far fa-save"></i> {{localize "MOJO.TraitSave"}}</button>
</form>
