<div class="form-group">
  <label>{{localize "MOJO.AdvancementCustomTitle"}}</label>
  <div class="form-fields">
    <input type="text" name="title" value="{{src.title}}" placeholder="{{default.title}}">
  </div>
</div>

<div class="form-group">
  <label>{{localize "MOJO.AdvancementCustomIcon"}}</label>
  <div class="form-fields">
    {{filePicker type="image" target="icon"}}
    <input type="text" name="icon" value="{{src.icon}}" placeholder="{{default.icon}}">
  </div>
</div>

{{#if showClassRestrictions}}
  <div class="form-group">
    <label>{{localize "MOJO.AdvancementClassRestriction"}}</label>
    <div class="form-fields">
      <select name="classRestriction">
        {{#select classRestriction}}
          <option value="">{{localize "MOJO.AdvancementClassRestrictionNone"}}</option>
          <option value="primary">{{localize "MOJO.AdvancementClassRestrictionPrimary"}}</option>
          <option value="secondary">{{localize "MOJO.AdvancementClassRestrictionSecondary"}}</option>
        {{/select}}
      </select>
    </div>
  </div>
{{/if}}

{{#if showLevelSelector}}
  <div class="form-group">
    <label>{{localize "MOJO.Level"}}</label>
    <div class="form-fields">
      <select name="level" data-dtype="Number">
        {{selectOptions levels selected=level}}
      </select>
    </div>
  </div>
{{/if}}
